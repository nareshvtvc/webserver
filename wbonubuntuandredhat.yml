---
 - hosts: [wbservers]
   become: yes
   tasks:
    - name: installing wbserver on ubuntu
      apt:
       name: "{{linuxserver}}"
       state: present
      when:   ansible_os_family =="Debian"
    - name: installing wbserver on redhat
      yum:
        name: "{{redhatsrever}}"
        state: present
      when:   ansible_os_family =="RedHat"
